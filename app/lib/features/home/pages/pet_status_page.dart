import 'package:flutter/material.dart';

class PetStatusPage extends StatelessWidget {
  const PetStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🐾 毛孩子状态'),
    );
  }
}
